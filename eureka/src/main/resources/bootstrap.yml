spring:
  application:
    name: eureka
server:
  port: 9001
eureka:
  server:
    enable-self-preservation: false
  #
  client:
    register-with-eureka: false
    fetch-registry: false
  instance:
    hostname: localhost